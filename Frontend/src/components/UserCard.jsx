import { Box } from "@mui/system";
import React from "react";

const UserCard = () => {
    return (
    <Box bgcolor="lightcoral" flex={1} padding={5}  sx={{display: {xs:"none", sm:"block"}}}>
      Usercard     
    </Box> );
}

export default UserCard;